<template>
  <div>
      <div class="switch-wrap">
        <label class="switch" for="loop-switch">
            <input id="loop-switch" type="checkbox" v-model="loopSwitchOn" @click="handleClick">    
            <span class="slider"></span>
        </label> {{loopSwitchOn ? 'On' : 'Off'}}
      </div>
  </div>
</template>

<script>
import {mapState, mapMutations} from 'vuex';
export default {
    methods: {
        ...mapMutations(['TOGGLE_LOOP_SWITCH']),
        handleClick() {
            this.TOGGLE_LOOP_SWITCH();
        },
    },
    computed: {
        ...mapState(['loopSwitchOn']),
    },
}
</script>

<style lang="scss" scoped>
    .switch-wrap {
        margin-bottom: 20px;
        font-size: 18px;
        font-weight: bold;
        text-transform: uppercase;
    }
    .switch input
    {
        display: none;
    }

    .switch 
    {
        display: inline-block;
        width: 60px;
        height: 30px;
        margin: 4px;
        transform: translateY(50%);
        position: relative;
    }

    .slider
    {
        position: absolute;
        top: 0;
        bottom: 0;
        left: 0;
        right: 0;
        border-radius: 30px;
        box-shadow: 0 0 0 2px #777, 0 0 4px #777;
        cursor: pointer;
        border: 4px solid transparent;
        overflow: hidden;
        transition: 0.2s;
    }

    .slider:before
    {
        position: absolute;
        content: "";
        width: 50%;
        height: 100%;
        background-color: #777;
        border-radius: 30px;
        transform: translateX(-30px);
        transition: 0.2s;
    }

    input:checked + .slider:before
    {
        transform: translateX(4px);
        background-color: limeGreen;
    }

    input:checked + .slider
    {
        box-shadow: 0 0 0 2px limeGreen, 0 0 8px limeGreen;
    }
</style>